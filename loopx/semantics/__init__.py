"""Repository-wide semantic vocabulary registry and inventory.

``vocabulary_v0.json`` is the curated registry (what each cross-module vocabulary
may carry, who defines it, how vocabularies relate). The inventory is computed
from tracked sources under ``loopx/``; optional reports are not authority.
Product code imports neither registry nor inventory; see the vocabulary RFC.
"""
